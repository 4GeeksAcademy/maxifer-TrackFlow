{
  "name": "trackflow-landing",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:css": "tailwindcss -i ./tailwind.input.css -o ./assets/tailwind.min.css --minify",
    "optimize:images": "node ./scripts/optimize-hero.mjs",
    "build:landing": "npm run build:css && npm run optimize:images"
  },
  "devDependencies": {
    "sharp": "^0.33.5",
    "tailwindcss": "^3.4.17"
  }
}
